// Code generated by goa v3.0.10, DO NOT EDIT.
//
// calc gRPC client CLI support package
//
// Command:
// $ goa gen calc/design

package cli

import (
	calcc "calc/gen/grpc/calc/client"
	"flag"
	"fmt"
	"os"

	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `calc (add|resta|multiplicacion|division)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calc add --message '{
      "a": 5850431520333673251,
      "b": 1089204046671954241
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, interface{}, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcAddFlags       = flag.NewFlagSet("add", flag.ExitOnError)
		calcAddMessageFlag = calcAddFlags.String("message", "", "")

		calcRestaFlags       = flag.NewFlagSet("resta", flag.ExitOnError)
		calcRestaMessageFlag = calcRestaFlags.String("message", "", "")

		calcMultiplicacionFlags       = flag.NewFlagSet("multiplicacion", flag.ExitOnError)
		calcMultiplicacionMessageFlag = calcMultiplicacionFlags.String("message", "", "")

		calcDivisionFlags       = flag.NewFlagSet("division", flag.ExitOnError)
		calcDivisionMessageFlag = calcDivisionFlags.String("message", "", "")
	)
	calcFlags.Usage = calcUsage
	calcAddFlags.Usage = calcAddUsage
	calcRestaFlags.Usage = calcRestaUsage
	calcMultiplicacionFlags.Usage = calcMultiplicacionUsage
	calcDivisionFlags.Usage = calcDivisionUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "add":
				epf = calcAddFlags

			case "resta":
				epf = calcRestaFlags

			case "multiplicacion":
				epf = calcMultiplicacionFlags

			case "division":
				epf = calcDivisionFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(cc, opts...)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcc.BuildAddPayload(*calcAddMessageFlag)
			case "resta":
				endpoint = c.Resta()
				data, err = calcc.BuildRestaPayload(*calcRestaMessageFlag)
			case "multiplicacion":
				endpoint = c.Multiplicacion()
				data, err = calcc.BuildMultiplicacionPayload(*calcMultiplicacionMessageFlag)
			case "division":
				endpoint = c.Division()
				data, err = calcc.BuildDivisionPayload(*calcDivisionMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers.
Usage:
    %s [globalflags] calc COMMAND [flags]

COMMAND:
    add: Add implements add.
    resta: Resta implements resta.
    multiplicacion: Multiplicacion implements multiplicacion.
    division: Division implements division.

Additional help:
    %s calc COMMAND --help
`, os.Args[0], os.Args[0])
}
func calcAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc add -message JSON

Add implements add.
    -message JSON: 

Example:
    `+os.Args[0]+` calc add --message '{
      "a": 5850431520333673251,
      "b": 1089204046671954241
   }'
`, os.Args[0])
}

func calcRestaUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc resta -message JSON

Resta implements resta.
    -message JSON: 

Example:
    `+os.Args[0]+` calc resta --message '{
      "a": 8614350958614154271,
      "b": 4285228716133201605
   }'
`, os.Args[0])
}

func calcMultiplicacionUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc multiplicacion -message JSON

Multiplicacion implements multiplicacion.
    -message JSON: 

Example:
    `+os.Args[0]+` calc multiplicacion --message '{
      "a": 6525775414444532386,
      "b": 5635247262153220700
   }'
`, os.Args[0])
}

func calcDivisionUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc division -message JSON

Division implements division.
    -message JSON: 

Example:
    `+os.Args[0]+` calc division --message '{
      "a": 5487677923305002774,
      "b": 1187293393747494935
   }'
`, os.Args[0])
}
