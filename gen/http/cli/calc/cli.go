// Code generated by goa v3.0.10, DO NOT EDIT.
//
// calc HTTP client CLI support package
//
// Command:
// $ goa gen calc/design

package cli

import (
	calcc "calc/gen/http/calc/client"
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `calc (add|resta|multiplicacion|division)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calc add --a 5401762099778430809 --b 1918630006328122782` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcAddFlags = flag.NewFlagSet("add", flag.ExitOnError)
		calcAddAFlag = calcAddFlags.String("a", "REQUIRED", "Left operand")
		calcAddBFlag = calcAddFlags.String("b", "REQUIRED", "Right operand")

		calcRestaFlags = flag.NewFlagSet("resta", flag.ExitOnError)
		calcRestaAFlag = calcRestaFlags.String("a", "REQUIRED", "Left operand")
		calcRestaBFlag = calcRestaFlags.String("b", "REQUIRED", "Right operand")

		calcMultiplicacionFlags = flag.NewFlagSet("multiplicacion", flag.ExitOnError)
		calcMultiplicacionAFlag = calcMultiplicacionFlags.String("a", "REQUIRED", "Left operand")
		calcMultiplicacionBFlag = calcMultiplicacionFlags.String("b", "REQUIRED", "Right operand")

		calcDivisionFlags = flag.NewFlagSet("division", flag.ExitOnError)
		calcDivisionAFlag = calcDivisionFlags.String("a", "REQUIRED", "Left operand")
		calcDivisionBFlag = calcDivisionFlags.String("b", "REQUIRED", "Right operand")
	)
	calcFlags.Usage = calcUsage
	calcAddFlags.Usage = calcAddUsage
	calcRestaFlags.Usage = calcRestaUsage
	calcMultiplicacionFlags.Usage = calcMultiplicacionUsage
	calcDivisionFlags.Usage = calcDivisionUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "add":
				epf = calcAddFlags

			case "resta":
				epf = calcRestaFlags

			case "multiplicacion":
				epf = calcMultiplicacionFlags

			case "division":
				epf = calcDivisionFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcc.BuildAddPayload(*calcAddAFlag, *calcAddBFlag)
			case "resta":
				endpoint = c.Resta()
				data, err = calcc.BuildRestaPayload(*calcRestaAFlag, *calcRestaBFlag)
			case "multiplicacion":
				endpoint = c.Multiplicacion()
				data, err = calcc.BuildMultiplicacionPayload(*calcMultiplicacionAFlag, *calcMultiplicacionBFlag)
			case "division":
				endpoint = c.Division()
				data, err = calcc.BuildDivisionPayload(*calcDivisionAFlag, *calcDivisionBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers.
Usage:
    %s [globalflags] calc COMMAND [flags]

COMMAND:
    add: Add implements add.
    resta: Resta implements resta.
    multiplicacion: Multiplicacion implements multiplicacion.
    division: Division implements division.

Additional help:
    %s calc COMMAND --help
`, os.Args[0], os.Args[0])
}
func calcAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc add -a INT -b INT

Add implements add.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calc add --a 5401762099778430809 --b 1918630006328122782
`, os.Args[0])
}

func calcRestaUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc resta -a INT -b INT

Resta implements resta.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calc resta --a 5855163322465186600 --b 1385266597691519195
`, os.Args[0])
}

func calcMultiplicacionUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc multiplicacion -a INT -b INT

Multiplicacion implements multiplicacion.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calc multiplicacion --a 7309877832173772408 --b 3237209857320107068
`, os.Args[0])
}

func calcDivisionUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc division -a INT -b INT

Division implements division.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calc division --a 9092197569596372935 --b 2937026320033011362
`, os.Args[0])
}
